@model TaskQuest.ViewModels.TelefoneViewModel

@using (Html.BeginForm("ExcluirTelefone", "Home", FormMethod.Post))
{
    @Html.HiddenFor(m => m.Id)
    <input type="submit" class="btn btn-primary btn-lg btn-header darkblue-bg" value="Excluir" />
}